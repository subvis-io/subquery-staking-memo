type Staking @entity {
  id: ID!
  blockNum: Int! @index
  timestamp: Date! @index
  stakingAmount: BigInt!
}

type Chronicle @entity {
  id: ID! # Static 'ChronicleKey'
  curBlockNum: Int
  curEra: Int
  curTotalIssuance: BigInt # TODO: confirm whether record all changes
  curAuctionCounter: Int # TODO: confirm whether record all changes
}
